# Secrets — never commit credential values
.env
.env.*
!.env.example
companies/*/secrets*

# Fleet — real filled-in registry stays local; only the .example placeholder is committed
fleet-registry.json
**/fleet-registry.json
templates/fleet-registry.json
ssh-config
!templates/ssh-config.example

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/

# OS/editor noise
.DS_Store
Thumbs.db
