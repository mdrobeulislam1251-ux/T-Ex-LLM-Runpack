# Runbook 07 — Product Deployment

```bash
bash scripts/setup.sh --port 3006
python3 -m texllm.cli serve
```

Health: `/health` · Auth profiles · Tailnet or domain.

Agent: Deploy Agent · Team: `tech`, `ops`
